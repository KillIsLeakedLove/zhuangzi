# 庄子网站项目开发规范

## 项目结构
```
project/
├── index.html          # 主页面入口
├── main.js             # 核心逻辑
├── styles.css          # 样式表
├── content/            # 章节数据
│   ├── xiaoyaoyou.js   # 章节名拼音.js
│   ├── qiwulun.js
│   └── ...
└── .clinerules         # 本文件
```

## 开发原则

### 1. HTML 结构规范
- 使用语义化标签
- 数据属性用于状态标记：`data-tab`、`data-index` 等
- 标签页结构：
```html
<div class="tabs">
    <div class="tab-nav">
        <button class="tab-btn active" data-tab="xxx">标签名</button>
    </div>
    <div class="tab-content">
        <div class="tab-panel active" id="tab-xxx">内容</div>
    </div>
</div>
```

### 2. JavaScript 规范
- 事件委托优于单独绑定
- **原文展开/收起**：点击整个 `.sentence` 容器触发，不只是 `.original`
```javascript
// 正确：事件绑定在容器上
<div class="sentence" onclick="toggleTranslation(index)">
    <p class="original">原文</p>
    <p class="translation">译文</p>
</div>
```
- 使用 CSS 变量支持主题切换
- 标签切换函数：`switchTab(tabId)` 统一处理激活状态

### 3. CSS 规范
- 使用 CSS 变量定义颜色，支持暗黑/明亮模式
- 交互元素需要 `:hover` 和 `:active` 状态
- 过渡动画使用 `transition: all 0.3s ease`
- 滚动容器隐藏默认滚动条但保留功能

### 4. 内容数据规范（content/*.js）
每个章节文件结构：
```javascript
window.chapterName = {
    "title": "章节名",
    "section": "内篇/外篇/杂篇",
    "sentences": [
        {
            "original": "原文",
            "translation": "译文"
        }
    ],
    "interpretation": "<p>解读内容</p>",
    "insight": "<p>启示内容（5点）</p>",
    "example": "<p>用例内容</p>"
}
```

**用例编写规范**：
- 使用**经典历史人物故事**，不编造现代虚构人物
- 优先选择：史书有载的历史人物、典故、成语故事
- 结构：人物+事迹+与庄子思想的关联
- 每章3个故事，涵盖不同角度

**优质用例来源**：
- 《史记》人物列传
- 春秋战国诸子典故
- 魏晋名士风流
- 唐宋文人轶事

### 5. 交互设计模式
1. **标签页**：点击切换，激活状态有视觉指示（下划线/背景色）
2. **展开/收起**：点击容器触发，有淡入动画
3. **目录导航**：侧边栏固定，支持折叠展开
4. **主题切换**：深色/浅色模式，保存到 localStorage

### 6. 文本样式规范
- 解读、启示、用例使用相同样式：`color: var(--text-secondary)`
- 段落间距：`margin-bottom: 12px`
- 行高：`line-height: 1.9`
- 引用经典用 `<strong>` 标签突出人物或主题

### 7. 响应式设计
- 移动端侧边栏移至顶部
- 字体大小适配：`font-size: 0.95rem`（正文）
- 触摸目标最小尺寸：44px

## 常见修改场景

### 添加新章节
1. 创建 `content/xxx.js`
2. 在 `index.html` 中添加 `<script>` 引用
3. 在 `main.js` 的 `chapters` 和 `chapterData` 中添加对应项

### 修改交互范围
- 事件绑定在**容器**而非子元素
- 保持与 `:hover` 效果范围一致

### 精修用例内容
- 检查：是否使用真实历史人物？
- 检查：是否体现本章核心思想？
- 检查：故事是否有史料依据？